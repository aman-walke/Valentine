<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Happy Valentine's Day ❤️</title>
    <style>
        body {
            background: linear-gradient(to right, #ff758c, #ff7eb3);
            text-align: center;
            font-family: 'Arial', sans-serif;
            color: white;
            overflow-x: hidden;
        }
        
        h1 {
            margin-top: 50px;
            font-size: 3rem;
            text-shadow: 2px 2px 5px rgba(0,0,0,0.3);
        }

        .hearts {
            position: absolute;
            width: 100%;
            height: 100%;
            pointer-events: none;
        }

        .heart {
            position: absolute;
            width: 20px;
            height: 20px;
            background: red;
            transform: rotate(-45deg);
            animation: float 5s linear infinite;
        }

        .heart::before, .heart::after {
            content: "";
            position: absolute;
            width: 20px;
            height: 20px;
            background: red;
            border-radius: 50%;
        }

        .heart::before { top: -10px; left: 0; }
        .heart::after { left: 10px; top: 0; }

        @keyframes float {
            0% { transform: translateY(0) rotate(-45deg); opacity: 1; }
            50% { opacity: 0.7; }
            100% { transform: translateY(-100vh) rotate(-45deg); opacity: 0; }
        }

        .message {
            margin-top: 30px;
            font-size: 1.5rem;
            max-width: 600px;
            margin-left: auto;
            margin-right: auto;
            background: rgba(255, 255, 255, 0.2);
            padding: 15px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0,0,0,0.2);
        }

        .gallery {
            margin-top: 20px;
            display: flex;
            justify-content: center;
            flex-wrap: wrap;
        }

        .gallery img {
            width: 150px;
            height: 150px;
            margin: 10px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0,0,0,0.3);
        }

        .surprise-btn {
            margin-top: 20px;
            padding: 10px 20px;
            font-size: 1.2rem;
            background: white;
            border: none;
            cursor: pointer;
            border-radius: 5px;
            box-shadow: 2px 2px 5px rgba(0,0,0,0.3);
        }

        .hidden-message {
            display: none;
            margin-top: 20px;
            font-size: 1.5rem;
            font-weight: bold;
            color: white;
        }
    </style>
</head>
<body>

    <h1>Happy Valentine's Day, My Love! ❤️</h1>
    
    <div class="hearts"></div>
    
    <p class="message">You make every moment special, and I feel so lucky to have you in my life. On this Valentine's Day, I just want to remind you how much I love you. ❤️</p>

    <div class="gallery">
        <!-- Add your images here -->
        <img src="your-image1.jpg" alt="Memory 1">
        <img src="your-image2.jpg" alt="Memory 2">
        <img src="your-image3.jpg" alt="Memory 3">
    </div>

    <button class="surprise-btn" onclick="showSurprise()">Click for a Surprise 💖</button>

    <p class="hidden-message">You're the most beautiful thing that ever happened to me! 💕</p>

    <script>
        function createHeart() {
            const heart = document.createElement("div");
            heart.className = "heart";
            heart.style.left = Math.random() * 100 + "vw";
            heart.style.animationDuration = Math.random() * 2 + 3 + "s";
            document.querySelector(".hearts").appendChild(heart);

            setTimeout(() => {
                heart.remove();
            }, 5000);
        }

        setInterval(createHeart, 300);

        function showSurprise() {
            document.querySelector(".hidden-message").style.display = "block";
        }
    </script>

</body>
</html>
